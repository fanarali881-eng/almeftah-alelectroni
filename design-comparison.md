# Design Comparison

The ManageAccount page has a green/yellow gradient line on the LEFT and title text on the RIGHT.
The BasicRegistration page in the original screenshot has a GRAY bar on the LEFT and title on the RIGHT with a blue right border.

Key differences from current implementation:
- Current: gray bar fills remaining space (flex:1), title has white background and blue right border
- Original screenshot: similar but the gray bar seems lighter and the overall bar has no gap from navbar
- The title bar should be full-width with no container padding
